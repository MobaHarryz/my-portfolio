/**
 * Static demo published inside the portfolio (GitHub Pages): no server or database.
 * Every API call is answered in the browser by core/demo-backend.ts and data is kept in localStorage.
 */
export const environment = {
  production: true,
  demo: true,
  apiUrl: 'demo-api',
};
