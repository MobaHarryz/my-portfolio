export const environment = {
  production: false,
  /** true = answer API calls in the browser (see core/demo-backend.ts) instead of calling the Spring Boot API */
  demo: false,
  apiUrl: 'http://localhost:8080/api',
};
