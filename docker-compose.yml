services:
  mongodb:
    image: mongo:8
    container_name: clinic-mascotas-mongo
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db
    restart: unless-stopped

volumes:
  mongo-data:
